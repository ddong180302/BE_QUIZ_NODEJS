

const createParticipantQuiz = () => {
    return new Promise((resolve, reject) => {
        try {

        } catch (e) {
            reject(e);
        }
    })
}

module.exports = {
    createParticipantQuiz,
}